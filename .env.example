# Docker environment variables
COMPOSER_ALLOW_SUPERUSER=1

# User/Group IDs for container user
# Use your host user ID and group ID for better file permissions
# You can find these by running: id -u && id -g
USER_ID=1001
GROUP_ID=1001

# Application environment
APP_ENV=dev

# Optional: PHP settings
PHP_MEMORY_LIMIT=256M
